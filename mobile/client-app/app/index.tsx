import { ScrollView } from "react-native";
import HomeContent from "./homeContent";

export default function Index() {
  return (
  <ScrollView>
  {/* <DebugDashboard /> */}
  <HomeContent />
  
  </ScrollView>
  );
}