import { ScrollView } from "react-native";
import HomeContent from "./homeScreen";

export default function Index() {
  return (
    <ScrollView>
      {/* <DebugDashboard /> */}
      <HomeContent />
    </ScrollView>
  );
}
