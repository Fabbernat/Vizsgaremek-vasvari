import { ScrollView } from "react-native";
import HomeScreen from "./homeScreen";

export default function Index() {
  return (
    <ScrollView>
      {/* <DebugDashboard /> */}
      <HomeScreen />
    </ScrollView>
  );
}
